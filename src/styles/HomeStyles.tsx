import { StyleSheet } from "react-native";

export default StyleSheet.create({
    resourcesContainer: {
        marginTop: '7vh',
        marginBottom: '3vh',
        alignItems: 'center',
    },
});