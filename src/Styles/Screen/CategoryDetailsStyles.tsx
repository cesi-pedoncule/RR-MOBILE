import { StyleSheet } from "react-native";

export default StyleSheet.create({
    resourcesContainer: {
        alignItems: 'center',
        justifyContent: 'center',
        marginTop: 10,
        marginBottom: 100,
    },
    headerContainer: {
        marginBottom: 20,
    }
});