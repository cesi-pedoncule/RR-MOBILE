import { StyleSheet } from "react-native";

export default StyleSheet.create({
    categoriesContainer: {
		paddingBottom: 100,
    },
    columnWrapperStyle: {
        marginHorizontal: 20,
        justifyContent: 'center',
    }
});