import { StyleSheet } from "react-native";

export default StyleSheet.create({
    categoriesContainer: {
		  paddingBottom: 80,
    },
    columnWrapperStyle: {
        marginHorizontal: 20,
        justifyContent: 'center',
    }
});